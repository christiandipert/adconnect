{
  "name": "adconnect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MVP prototype for interactive sponsored offer connectors in Codex and ChatGPT.",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "node --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
